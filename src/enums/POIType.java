package enums;

/**
 * Used to describe different kinds of points of interests (POI).
 */
public enum POIType {
    ALERT, BAR, BICYCLE, CAMERA, CAR, CITY, FOOD, FUEL, HOUSE, JOB, PARKING, SHOP, STAR, UNKNOWN;
}
